# 参与贡献

欢迎补充其他校区、设备的配置和测试结果。Issue 请附固件版本、复现步骤和脱敏后的最小配置；性能数据注明服务器路径、连接数、时长及空载/满载指标。

修改后运行：

```sh
npm test
npm run generate
```

Shell 脚本另做 `sh -n` 或 `bash -n` 检查，依赖 LuCI/nixio 的脚本需在目标环境测试。新增网站分类应同步更新 catalog、规则 manifest 和优先级测试。

请勿提交账号、密钥、完整配置备份或抓包。引用上游代码时保留来源和许可证。
