# Contributing

欢迎补充不同校区、硬件与固件的验证。请写清楚版本、测试路径、负载、空载/满载采样、是否重启、实际应用反馈和回滚状态。

请将提案与已验证结论分开，不把账号套餐、PHY速率、接口速率和应用有效载荷混为一谈。不要提交原始私人日志或固件/校准二进制。涉及网络行为的修改，应提供最小配置差异及不影响已有连接、DNS、PBR和CAKE的证据。

运行 `npm test`、`npm run generate`；参考 shell 另用 `sh -n` 或 `bash -n` 检查。测试不连接真实路由器。带依赖的 LuCI/nixio 脚本需要目标环境验证，不能仅凭静态检查宣称已部署。

新增规则类别请更新 catalog、固定规则manifest及优先级测试；自定义域名和具体服务必须优先于适当的大类，保留校园保护。对上游代码保留许可证和来源。
